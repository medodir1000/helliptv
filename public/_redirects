# API → Netlify Functions. MUST come before the SPA catch-all, otherwise
# /api/* gets rewritten to /index.html (the bug that 404'd all /api endpoints).
/api/*    /.netlify/functions/:splat   200

# SPA fallback — every other route is handled client-side by React Router.
/*        /index.html                  200
